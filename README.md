## Where we put all files needed for packaging

### Debian packages

Due to some changes in packages availabilty in debian 13 (trixie), le deb directory now contains two directory "oldstables" / "trixie"